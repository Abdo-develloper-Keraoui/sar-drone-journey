#include <stdio.h>

int main(void) {

	/*
	int c;

	while((c = getchar()) != EOF){
		putchar(c);
	}*/

	//printf("(getchar() != EOF) === %d", (getchar() != EOF));

	getchar();

	printf("%d\n", EOF);
	return 0;
}
